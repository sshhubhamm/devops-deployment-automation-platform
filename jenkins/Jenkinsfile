pipeline {
    agent any

    stages {
        stage('Install Dependencies') {
            steps {
                bat 'python -m pip install -r app/requirements.txt'
            }
        }

        stage('Test Application') {
            steps {
                bat 'python -c "print(\'CI pipeline executed successfully\')"'
            }
        }
    }
}
